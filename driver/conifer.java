package driver;

import environment.*;
import info.*;

public class conifer {
    public static void main(String[] args) {

        // create the ui
        gui env = new gui();
        env.main(args);
    }
}
